<UserControl x:Class="SystemHealthMonitor.WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SystemHealthMonitor.WPF.Views"
             xmlns:controls="clr-namespace:SystemHealthMonitor.WPF.Views.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
    <Grid>
       <!-- Header-->
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:ViewHeader Grid.Row="0" />
        <!-- Content -->
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!-- Add tab for worst performing metrics last 30 days-->
            <controls:ResultsCard  Grid.Column="0" CardTitle="Top 3 Worst Results" CardItems="{Binding WorstResults}" />
            <controls:ResultsCard Grid.Column="1" CardTitle="Latest Results" CardItems="{Binding LatestResults}" />
        </Grid>
    </Grid>

</UserControl>
