<UserControl x:Class="SystemHealthMonitor.WPF.Views.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SystemHealthMonitor.WPF.Views"
             xmlns:controls="clr-namespace:SystemHealthMonitor.WPF.Views.CustomControls"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Filter Button -->
        <Style x:Key="FilterClearButton" TargetType="{x:Type Button}">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
            <Setter Property="Background" Value="#008190"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Width" Value="90" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" TextBlock.Foreground="{TemplateBinding Foreground}" TextBlock.FontSize="{TemplateBinding FontSize}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2E8FA3" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Pagination Button -->
        <Style x:Key="PaginationButton" TargetType="{x:Type Button}">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                    <Setter Property="BorderBrush" Value="#A5D4DD"/>
                </Style>
            </Style.Resources>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="Padding" Value="3" />
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Width" Value="70" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" TextBlock.Foreground="{TemplateBinding Foreground}" TextBlock.FontSize="{TemplateBinding FontSize}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C6E7EC" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- DateTime Picker -->
        <Style x:Key="DatePicker" TargetType="{x:Type DatePicker}">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="20,3,0,0" />
            <Setter Property="Width" Value="160" />
            <Setter Property="Height" Value="40" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="FirstDayOfWeek" Value="Monday" />
            <Setter Property="BorderBrush" Value="#A5D4DD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="CalendarStyle" Value="{DynamicResource DateTimePickerCalendar}" />
        </Style>
        <!-- Calendar -->
        <Style x:Key="DateTimePickerCalendar" TargetType="{x:Type Calendar}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Calendar}">
                        <Viewbox Height="250">
                            <CalendarItem x:Name="PART_CalendarItem" Background="White" BorderBrush="#A5D4DD" BorderThickness="1" />
                        </Viewbox>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DateTimePickerCalendarIcon" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePicker}">
                        <Button x:Name="PART_Button" Focusable="False" Grid.Column="1">
                            <Button.Template>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Image Source="/Icons/SystemHealthMonitor.ico" />
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ResultsDataGrid" TargetType="{x:Type DataGrid}">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
            <Setter Property="ColumnHeaderStyle" Value="{DynamicResource ColumnHeaderStyle}" />
            <Setter Property="CellStyle" Value="{DynamicResource DataGridCellStyle}" />
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="AlternatingRowBackground" Value="#99A5D4DD"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowBackground" Value="WhiteSmoke"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="Margin" Value="20,18,20,9"/>
            <Setter Property="BorderBrush" Value="#A5D4DD" />

        </Style>
        <!-- DataGridColumnHeader style -->
        <Style x:Key="ColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#008190" />
            <Setter Property="BorderBrush" Value="#A5D4DD"/>
            <Setter Property="BorderThickness" Value="1,0,0,1"/>
            <Setter Property="SeparatorVisibility" Value="Visible"/>
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
        <!-- DataGridCellStyle style -->
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="7, 10, 10, 10" />
            <Setter Property="Background" Value="Transparent"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
        </Style>
        <!-- PageInfo style -->
        <Style x:Key="PageInfo" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0, 5, 20, 20" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>
        <controls:ViewHeader  Grid.Row="0"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*" />
                <ColumnDefinition Width="30*" />
                <ColumnDefinition Width="40*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Margin="0, 0, 10, 0" Style="{DynamicResource FilterClearButton}" Command="{Binding FilterResultsCommand}">Filter</Button>
                <Button Margin="0 ,0, 20, 0" Style="{DynamicResource FilterClearButton}" Command="{Binding ClearFilterResultsCommand}">Clear</Button>
            </StackPanel>
            <DatePicker Grid.Column="0"  Style="{DynamicResource DatePicker}" SelectedDate="{Binding SelectedStartDate}" DisplayDateEnd="{x:Static sys:DateTime.Today}" DisplayDate="{x:Static sys:DateTime.Today}"/>
            <DatePicker Grid.Column="1" Style="{DynamicResource DatePicker}" SelectedDate="{Binding SelectedEndDate}" DisplayDateEnd="{x:Static sys:DateTime.Today}" DisplayDate="{x:Static sys:DateTime.Today}"/>
        </Grid>
        <Grid Grid.Row="2">
            <DataGrid Style="{DynamicResource ResultsDataGrid}" ItemsSource="{Binding PageResults}" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Width="30*" Binding="{Binding DateTime, StringFormat=\{0:dd-MM-yy HH:mm:ss\}}"/>
                    <DataGridTextColumn Header="Metric" Width="30*" Binding="{Binding Metric}" />
                    <DataGridTextColumn Header="Value (%)" Width="40*"  Binding="{Binding Value}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Grid.Column="0" Style="{DynamicResource PageInfo}" Text="{Binding CurrentPageNumber}"/>
                <Button Grid.Column="0" Margin="0, 0, 5, 20" Style="{DynamicResource PaginationButton }" Command="{Binding PreviousPageCommand}">&lt;</Button>
                <Button Grid.Column="0" Margin="0, 0, 18, 20" Style="{DynamicResource PaginationButton}" Command="{Binding NextPageCommand}">&gt;</Button>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
